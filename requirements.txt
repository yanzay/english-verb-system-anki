
# Tier-2 (audio + IPA). All optional — build_anki_package.py works without
# them; the deck simply ships without media if media/ is empty.
google-cloud-texttospeech>=2.16.0   # for build_audio.py
eng-to-ipa>=0.0.2                   # for build_ipa.py
anki>=24.0
zstandard>=0.22
